# local dev helper (static preview server)
server.mjs

# tooling / OS
.claude/
node_modules/
.DS_Store
Thumbs.db
.vercel

.vercel
.env*

# content working files — the page itself is the source of truth
mentions*.csv
mentions.xlsx
